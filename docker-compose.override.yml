version: '3.8'

services:
  backend:
    environment:
      # Force CORS to allow all origins
      CORS_ALLOWED_ORIGINS: "*"
      # Additional debugging
      LOG_LEVEL: DEBUG
    command: uvicorn app.main:app --host 0.0.0.0 --port 50000 --reload --log-level debug